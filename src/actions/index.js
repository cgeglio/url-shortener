export const setUrls = (urls) => ({
  type: 'SET_URLS',
  urls
});

export const addUrl = (url) => ({
  type: 'ADD_URL',
  url
});

export const deleteUrl = (url) => ({
  type: 'DELETE_URL',
  url
});
